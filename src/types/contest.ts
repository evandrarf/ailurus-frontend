export interface ContestInfo {
  event_name: string;
  event_status: string;
  current_round: number;
  current_tick: number;
  start_time: string;
  number_round: number;
  number_tick: number;
  tick_duration: number;
  logo_url: string;
}
